module Findable
  
        def find_by_name(name)
            self == Brand_names ? Brand_names.all.detect{|b| b.brand_name == name} #: Cigars.all.detect{|c| c.cigar_name == name}            
        end
    
end